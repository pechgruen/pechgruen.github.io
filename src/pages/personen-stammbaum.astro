---
import BaseLayout from "../layouts/BaseLayout.astro";
import PersonenRecherche from "../components/PersonenRecherche.astro";

export const chronik = {
  title: "Genealogische Liste",
  subtitle: "Alle im Stammbaum erfassten Personen – frei filterbar",
  date: "2025-12-22",
  updated: "2026-01-25",
  bereich: "Recherche",
  href: "/personen-stammbaum",
  order: 1,
};
---

<BaseLayout title=chronik.title>
  <header class="head">
    <h1>{chronik.title}</h1>
    <p class="subtitle">{chronik.subtitle}</p>
    <p class="meta">{chronik.date}</p>
  </header>

  <PersonenRecherche
    pageTitle=chronik.title
    personsUrl="/data/pechgruen-ged.csv"
    housesUrl="/data/haeuser.csv"
  >
    <p>
      Diese Seite ist ein <strong>Recherche-Werkzeug</strong> auf Basis von <strong>Udos Stammbaum</strong>.
      Jede Zeile entspricht einem <strong>Datensatz</strong> im gleichen Tabellenformat wie beim Einwohnermeldebuch,
      aber die Quelle ist hier der Stammbaum-Export (Pechgrün/Neuhäuser-Fokus) – nicht das historische Register.
    </p>

    <h2 class="subhead">Hauskennungen (P…, N…, K…)</h2>

    <ul>
      <li><strong>P</strong> = Pechgrün</li>
      <li><strong>N</strong> = Neuhäuser (seit <strong>1924</strong> eingemeindet)</li>
    </ul>

    <p>
      Falls keine Hausnummer bekannt ist, wird <strong>P000</strong> bzw. <strong>N000</strong> als Platzhalter verwendet.
      Ein Klick auf die Hauskennung öffnet das Haus-Popup, ein Klick in die Zeile das Personen-Popup.
    </p>
  </PersonenRecherche>

  <!-- IMPORTANT: global so dynamically injected <a> also match (no Astro scoping) -->
  <style is:global>
    /* --- page header --- */
    .{chronik.title} .head {
      margin-bottom: 1.6rem; /* Header → Body */
    }

    .{chronik.title} .head h1 {
      margin-bottom: 0.25rem; /* Titel → Subtitle */
    }

    .{chronik.title} .head .subtitle {
      margin-top: 0;
      margin-bottom: 0.4rem; /* Subtitle → Meta */
      color: rgba(0, 0, 0, 0.65);
    }

    .{chronik.title} .head .meta {
      margin: 0; /* Meta → nichts extra */
      font-size: 0.85rem;
      color: rgba(0, 0, 0, 0.45);
    }
  </style>
</BaseLayout>

